slots:
  data_types:
    kind: "system"
    entity_type: "data_type"
    required: false
    resolver: "observation_availability"
    prompt: "Select indicator domain"
    multi: false

  sensors:
    kind: "system"
    entity_type: "sensor"
    required: true
    resolver: "observation_availability"
    prompt: "Select sensors"
    multi: true

  test_segments:
    kind: "system"
    entity_type: "test_segment"
    required: true
    resolver: "observation_availability"
    prompt: "Select test segments"
    multi: true

  indicator_names:
    kind: "system"
    entity_type: "indicator"
    required: false
    resolver: "observation_indicators"
    prompt: "Select indicators"
    multi: false
